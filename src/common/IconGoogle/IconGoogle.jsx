import React from "react";

const IconGoogle = ({ children }) => {
    return (
        <>
            <span class="material-symbols-outlined">{children}</span>
        </>
    );
};

export default IconGoogle;
